# Requirements

This project can be run as is. All data sources and the required libraries (d3, scrollama) are saved locally.
However, due to browser CORS policies it must be served through a local web server.

Recommended: WebStorm web server
If you are using WebStorm, open the project and start it using WebStorm's built-in web server.

Alternative: Node Web server

